/**
 * Bundled Aperture 1.8.2 — reimplemented inside this mod, original namespace kept.
 *
 * <p>Legacy source of truth:
 * {@code .tools/legacy-src/aperture/src/main/java/mchorse/aperture/}. Holds
 * the {@code CameraProfile} JSON model ({@code camera/fixtures/*},
 * {@code camera/modifiers/*}) and the camera playback runtime. Ported in the
 * S9-era camera stages; Blockbuster's own Aperture glue lives separately in
 * {@code mchorse.blockbuster.aperture}.</p>
 *
 * <p>Original {@code mchorse.aperture.*} names kept for diff-ability.</p>
 */
package mchorse.aperture;
