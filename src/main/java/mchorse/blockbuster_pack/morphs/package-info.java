/**
 * Blockbuster's custom morph implementations.
 *
 * <p>Legacy source of truth:
 * {@code blockbuster-1.12/src/main/java/mchorse/blockbuster_pack/morphs/}
 * — {@code CustomMorph}, {@code ImageMorph}, {@code ParticleMorph},
 * {@code RecordMorph}, {@code SequencerMorph}, {@code SnowstormMorph},
 * {@code StructureMorph}, {@code TrackerMorph}, {@code LabelMorph}, etc. Each
 * extends the bundled Metamorph {@code AbstractMorph} and is discriminated by
 * its {@code Name} NBT tag. Ported in roadmap stage S7.</p>
 *
 * <p>P6 skeleton placeholder — real classes land with S7; keep legacy
 * class names/NBT keys byte-identical.</p>
 */
package mchorse.blockbuster_pack.morphs;
