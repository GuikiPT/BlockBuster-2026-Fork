/**
 * Blockbuster's own morph pack (server/common side).
 *
 * <p>Legacy source of truth:
 * {@code blockbuster-1.12/src/main/java/mchorse/blockbuster_pack/}
 * (root classes {@code BlockbusterFactory}, {@code BlockbusterSection},
 * {@code MetamorphHandler}). These register Blockbuster's custom morphs into
 * the bundled Metamorph morph registry. Ported in roadmap stage S7 (see
 * {@code plan/S07-blockbuster-morphs.md}).</p>
 *
 * <p>Package/class names are kept identical to the 1.12.2 source for
 * diff-ability against the legacy tree (CROSS_CUTTING ground rule). This is a
 * P6 skeleton placeholder — real classes land with S7.</p>
 */
package mchorse.blockbuster_pack;
