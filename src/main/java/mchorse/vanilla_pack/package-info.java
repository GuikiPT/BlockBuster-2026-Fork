/**
 * Metamorph's builtin ("vanilla") morph pack — a root-level package.
 *
 * <p>Legacy source of truth:
 * {@code .tools/legacy-src/metamorph/src/main/java/mchorse/vanilla_pack/}.
 * Metamorph registers its builtin morphs (block, item, player, iron golem,
 * shulker, undead, label, …) from here — it is a sibling of
 * {@code mchorse.metamorph}, NOT a subpackage, and is easy to overlook when
 * mirroring the layout. Ported with the bundled Metamorph (S6).</p>
 *
 * <p>Original {@code mchorse.vanilla_pack.*} names kept for diff-ability.</p>
 */
package mchorse.vanilla_pack;
